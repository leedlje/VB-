# VB阅读器产品设计文档

## 产品方向

面向 Windows 10/11 x64 用户，提供简单、稳定、离线可用的本地电子书阅读体验。下一大版本将产品名称从“TXT 阅读器”改为“VB阅读器”，并升级为支持 TXT、EPUB、PDF 的统一书架。应用不要求账号，不上传或修改用户的书籍正文。

## 当前版本：TXT 阅读器（已实现）

- 打开本地 TXT，默认按 UTF-8 读取，可手动切换 GB18030；保留换行并自动换行。
- 按文件保存进度与书签，启动时恢复上次文件；提供最近阅读、文内搜索、明亮／深色／护眼主题、字号与排版调整。
- 支持重新定位已移动或改名的 TXT，以及打开文件和搜索的键盘快捷键。文件丢失、无法读取或内容变化时提供提示。

## 下一大版本：统一书架与多格式阅读

以“导入书籍 → 在书架中找到书 → 阅读 → 下次从原处继续”为核心流程。按统一书架、EPUB、PDF 的顺序实施，每一步都保留已有 TXT 数据和阅读能力。

### 1. 统一书架

- 支持一次选择多个 TXT、EPUB、PDF 文件导入。书架显示封面或占位图、书名、作者、格式和阅读进度；可按最近阅读或书名排序，按格式筛选并搜索书名。
- 优先读取文件自带的书名、作者和封面；信息缺失时使用文件名和默认封面。同一路径重复导入时复用原记录，不生成重复书籍。
- 导入默认**引用原文件路径**，不复制正文。移出书架只删除应用记录，不删除磁盘文件；原文件被移动时可重新定位并保留阅读记录。
- 将当前 TXT 阅读记录、书签和外观设置迁移到新书架，升级后用户仍能继续阅读原有书籍。

### 2. EPUB 阅读

- 支持常见的可重排 EPUB：按书籍顺序显示正文、图片和内链，使用 EPUB 自带目录跳转章节。
- 支持调整字号与适用的排版设置、文内搜索、书签和进度恢复。阅读位置按 EPUB 的内容结构记录，不用 TXT 的字符偏移代替。
- 固定版式、加密或受 DRM 保护的 EPUB 不在本次交付范围；遇到不支持的文件时给出明确提示。

### 3. PDF 阅读

- 提供按页阅读、页码跳转、缩放、适合页面和适合宽度；保存页码与页内位置，并支持按页添加书签。
- 对有文字层的 PDF 提供文内搜索。扫描版 PDF 可以阅读页面，但本版本不提供 OCR 或图片文字搜索。
- PDF 保持原页面排版；字号、行距等文字重排设置只用于 TXT 和可重排 EPUB。

### 4. 产品更名

- 将应用内标识、窗口标题、安装程序及安装后的程序名称、系统快捷方式和用户文档统一改为“VB阅读器”。升级安装后，原有书籍记录、进度、书签和设置继续可用。

## 大版本验收标准

- **统一书架**：混合批量导入 TXT、EPUB、PDF 后，书籍信息和格式正确；重复导入不产生重复记录；排序、筛选、搜索和移出书架正常。重启后记录仍在，原文件未被移动或删除。
- **EPUB 阅读**：目录、正文、图片和内链可用；修改排版或重启后能恢复合理的阅读位置；搜索和书签能跳回对应内容。
- **PDF 阅读**：翻页、页码跳转、缩放及适合页面／宽度可用；重启后恢复页码与页内位置；有文字层时能搜索，扫描版给出无法搜索文字的说明。
- **产品更名**：应用界面、窗口和安装后的系统入口均显示“VB阅读器”；从旧版升级后，原有阅读数据仍可访问。
- **数据与异常**：现有 TXT 进度、书签和设置在升级后保留；文件丢失、损坏或格式不受支持时，书架与阅读窗口继续可用，并提供下一步操作入口。
- **回归与性能**：三种格式的导入、阅读、书签和进度恢复通过自动化测试及真实窗口验证；常见长篇书籍的打开、切换和搜索不应让界面长时间无响应。

## 后续候选方向

拖入文件和 Windows 文件关联、复制书籍到应用管理的本地书库、跨书搜索、分类与标签、MOBI／AZW3／CBZ、扫描版 PDF 的 OCR、批注、朗读及云同步。TXT 自动章节识别仍是可选功能。这些方向依据实际使用反馈再安排，不属于本次大版本范围。

## 体验原则

- 书架和阅读器继续离线可用；保存封面缓存与必要的书籍元数据，不把整本正文复制到应用状态文件。
- 各格式使用适合自身的阅读位置表示，统一呈现进度、书签和错误提示。
- 每项新功能覆盖正常与关键异常场景，并通过测试和实际窗口验证。
