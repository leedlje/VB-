# 电子书阅读器产品设计文档

## 产品方向

面向阅读本地电子书的用户，提供简单、稳定、离线可用的阅读体验。先把 TXT 阅读做好，再逐步补充阅读管理、阅读辅助和其他格式。应用默认不要求账号，也不上传书籍正文。

当前产品面向 Windows 10/11 x64。后续版本仍以本地阅读为主，具体功能按使用反馈逐步确定。

## 当前版本：TXT 阅读 MVP（已实现）

用户可以选择本地 TXT 文件，在明亮主题下滚动阅读；正文保留换行并自动换行。应用支持调整字号、默认按 UTF-8 读取并手动切换 GB18030，还会分别保存每个文件的阅读位置，启动时尝试恢复上次打开的文件。

当前版本不提供书架、目录、搜索或书签。TXT 没有统一的章节结构，因此后续也不把自动识别章节作为基础阅读的前提。

## 下一阶段：让日常阅读更方便

按以下顺序逐项迭代，每项功能都应保留现有 TXT 阅读和进度恢复能力：

1. **最近阅读与简易书架**：列出用户打开过的本地文件、最近阅读时间和进度；可继续阅读或从列表中移除记录。移除记录不删除原文件，书籍正文仍保留在原位置。
2. **文内搜索**：搜索当前 TXT 的文字，显示匹配位置，支持跳到上一处或下一处结果；没有结果时明确提示。
3. **书签**：在当前阅读位置添加书签，查看、跳转和删除书签；书签按文件分别保存。
4. **阅读外观**：增加深色和护眼主题，并保存用户选择；可按实际反馈继续补充行距等排版设置。

这一阶段先服务 TXT 阅读，不要求登录、云端存储或章节识别。

## 更后续的扩展方向

- **EPUB**：在 TXT 体验稳定后评估支持，增加 EPUB 自带的目录导航，并处理与 TXT 不同的排版和进度定位。
- **PDF**：单独评估固定页面的显示、缩放与定位体验，不直接套用 TXT 的阅读方式。
- **批注与朗读**：根据真实使用需求决定是否加入，并考虑标记位置与正文变化后的对应关系。
- **云同步与账号**：只有在多设备阅读需求明确时再规划；先明确隐私、冲突处理和离线使用方式。

以上是候选方向，并非下一阶段的交付承诺。TXT 自动章节识别也可作为可选功能评估，不影响普通 TXT 文件的阅读。

## 体验原则与验收重点

- 打开、切换和恢复文件时保持清楚的状态反馈；文件丢失、空文件、无法读取或编码不正确时，用户能继续选择文件或切换编码。
- 调整字号、切换主题、搜索或跳转书签后，阅读位置应符合用户预期；重启应用后，已有进度和设置仍可恢复。
- 本地记录只保存实现功能所需的信息；不修改用户原始 TXT 文件，不将书籍正文复制到应用状态文件。
- 每项新功能都要覆盖正常使用和关键异常场景，并通过自动化测试及实际窗口验证。
