# 电子书阅读器产品设计文档

## 产品方向

面向 Windows 10/11 x64 用户，提供简单、稳定、离线可用的本地阅读体验。现阶段专注 TXT；应用不要求账号，不上传或修改用户的书籍正文。

## 当前版本：TXT 阅读与管理（已实现）

- **基础阅读**：打开本地 TXT，保留换行、自动换行并滚动阅读；默认使用 UTF-8，可手动切换 GB18030。
- **进度与外观**：按文件保存阅读位置，启动时尝试恢复上次文件；可调整字号，切换明亮、深色和护眼主题。
- **阅读管理**：最近阅读列表显示文件与进度，可继续阅读或移除记录；支持当前文件的文内搜索及逐文件书签。
- **异常提示**：文件丢失、无法读取或内容变化时给出提示。文件内容变化后，旧进度与书签位置可能不再精确。

## 下一小版本：完善 TXT 阅读体验

这一版本依次交付下面三项，保留现有功能和离线使用方式。

1. **重新定位文件**：最近阅读中的文件被移动或改名后，允许用户指定新的 TXT 路径。确认后沿用原记录中的编码、进度和书签；取消时保留旧记录。新路径不是可读 TXT 时，不覆盖原记录；若内容发生变化，提示原位置可能有偏差。
2. **键盘操作**：提供 `Ctrl+O` 打开文件、`Ctrl+F` 聚焦文内搜索、`F3` / `Shift+F3` 切换搜索结果，以及 `Esc` 关闭当前搜索或侧栏。快捷键不应干扰输入框中的文字编辑，主要操作仍可通过界面按钮完成。
3. **阅读排版设置**：允许调整行距和正文宽度，保存用户选择。调整设置或改变窗口大小后，尽量保持当前阅读位置，正文不被遮挡。

## 下一小版本的验收标准

- 文件移动后重新定位，重新启动应用仍能打开新路径，原进度和书签保留；取消或选错文件时不会丢失原记录，也不会修改原 TXT。
- 快捷键能完成打开、搜索和结果跳转；在搜索框输入文字时，常用编辑操作正常，按 `Esc` 可回到阅读界面。
- 行距和正文宽度调整后立即生效，重启后设置仍在；调整前后的阅读位置保持合理，窄窗口下可以正常阅读。
- 现有 TXT 打开、编码切换、最近阅读、搜索、书签、主题和进度恢复继续通过自动化测试及真实窗口验证。

## 后续候选方向

- **打开文件更方便**：拖入 TXT，以及从 Windows 文件管理器直接用阅读器打开 TXT。
- **大文件体验**：依据实际性能测试，优化长篇 TXT 的打开、搜索和滚动响应；改进文件内容变化后的定位。
- **格式扩展**：评估 EPUB 的目录与流式排版；另行设计 PDF 固定页面的阅读方式。
- **其他能力**：按用户需求评估批注、朗读、云同步和账号。TXT 自动章节识别属于可选功能，不作为基础阅读的前提。

以上均为后续候选方向，不属于下一小版本的交付范围。

## 体验原则

- 本地记录只保存实现功能所需的信息；书籍正文始终保留在用户原文件中。
- 文件丢失、编码不正确或状态损坏时，给出可理解的提示与继续操作的入口。
- 每项新功能都覆盖正常与关键异常场景，并通过测试和实际窗口验证。
