# TXT 电子书阅读器：MVP 产品设计文档

## 产品方向

做一款轻量的本地电子书阅读器。第一版只支持 TXT 文件，重点是让用户能顺利打开、阅读，并在下次打开时接着读。产品离线可用，不要求登录。

## 目标用户与使用流程

面向希望阅读本地 TXT 书籍的用户。核心流程是：选择 TXT 文件 → 阅读和调整字号 → 关闭应用 → 再次打开时从上次位置继续阅读。

## 第一版主要功能

1. **打开文件**：从本地选择并打开 TXT 文件；启动时可恢复上次阅读的文件。文件不存在或无法读取时，给出清楚的提示。
2. **文字编码**：默认按 UTF-8 读取；若出现乱码，用户可手动切换为 GB18030 后重新读取。
3. **基础阅读**：使用明亮主题显示正文，保留原有换行，长行自动换行，通过滚动浏览内容。
4. **字号调整**：提供简单的增大、减小字号操作。
5. **阅读进度**：自动保存每个文件的阅读位置；重新打开同一文件时恢复到上次位置。

## 暂不纳入第一版

EPUB、PDF 等其他格式；书架、目录和章节识别；搜索、书签、批注；护眼或深色主题；云同步、账号和朗读功能。

## 验收标准

- 可以打开 UTF-8 和 GB18030 编码的常见 TXT 文件，中文显示正常，换行和自动换行符合预期。
- 调整字号后仍能正常阅读；退出并重新打开文件后，阅读位置能够恢复。
- 空文件、文件丢失或读取失败时，应用给出提示，不直接崩溃。

## 后续待定

目标平台和技术方案尚未确定，开发前需要明确。后续功能根据实际阅读体验和用户反馈安排。
